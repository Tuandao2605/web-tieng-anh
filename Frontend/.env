# Base URL của API Backend (dùng Vite proxy nếu cùng origin)
# Để trống hoặc đặt "/api/v1" để dùng proxy nội bộ
VITE_API_BASE_URL=/api/v1

# Nếu Frontend và Backend deploy khác domain, điền URL đầy đủ:
# VITE_API_BASE_URL=http://localhost:3000/api/v1
